{
  "name": "your-node-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",  // Example start script
    "test": "mocha tests/*.js", // Example test script
    "build": "webpack --config webpack.config.js"  // Example build script
  },
  "dependencies": {
    // Your dependencies
  },
  "devDependencies": {
    // Your dev dependencies
  }
}
